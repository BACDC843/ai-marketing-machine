# [Business Name] — Content Calendar

Running log of organic social content — drafted, scheduled, and published. Appended to by every content skill that produces a post. Newest entries at the bottom.

`performance-digest` writes the Performance and Verdict columns back into this file after a run. **A metric that failed to fetch is left blank, never written as `0`** — a missing number and a measured zero mean opposite things, and a zero recorded today skews every median from here on.

| Date | Format | Channel | Summary | Status | File | Performance | Verdict |
|---|---|---|---|---|---|---|---|

## Benchmarks

Per-format medians, recalculated by `performance-digest`. Fewer than four measured posts in a format stays **provisional** — say so rather than presenting an early median as settled.

_No measured posts yet._
